{
  "build": { "builder": "NIXPACKS" },
  "deploy": {
    "startCommand": "python -m chainlit run app.py --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
